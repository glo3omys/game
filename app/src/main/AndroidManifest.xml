<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">


        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity android:name=".GameListActivity" />
        <activity android:name=".TaptapActivity" />
        <activity android:name=".BalloonActivity" />
        <activity android:name=".ScavengerHuntActivity" />
        <activity android:name=".MathActivity" />
        <activity android:name=".ScavListActivity" />
        <activity android:name=".FingerChoiceActivity" />
        <activity android:name=".InitialQuizActivity" />
        <activity android:name=".LeftRightActivity" />
        <activity android:name=".FindNumberActivity" />
        <activity android:name=".ConnTestActivity" />
        <activity android:name=".WhackAMoleActivity" />
        <activity android:name=".MemoryCardGameActivity" />
        <activity android:name=".SearchRoomActivity" />
        <activity android:name=".LobbyActivity" />
    </application>

</manifest>